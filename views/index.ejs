<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Mountains+of+Christmas:wght@700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css"> 
    <title><%= locals.lojaNome %></title> 
</head>
<body>

    <div class="promo-bar">
        <i class="bi bi-truck"></i>
        <strong>FRETE GRÁTIS</strong> em compras acima de R$ 30,00!
    </div>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/"><%= locals.lojaNome %></a> 
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item">
                        <a class="nav-link active" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="cart-link">
                            <i class="bi bi-cart"></i> Carrinho
                            <span class="badge bg-success" id="cart-counter" style="display: none;"></span>
                        </a>
                    </li>
                    <% if (locals.clienteLogado) { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownCliente" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-person-circle"></i> Olá, <%= locals.clienteLogado.nome %>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownCliente">
                                <li><a class="dropdown-item" href="/minha-conta">Minha Conta</a></li> 
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/logout-cliente">Sair</a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link" href="/login-cliente"><i class="bi bi-box-arrow-in-right"></i> Login</a></li>
                        <li class="nav-item"><a class="nav-link" href="/register"><i class="bi bi-person-plus-fill"></i> Cadastre-se</a></li>
                    <% } %>
                    <% if (locals.isAdmin) { %><li class="nav-item"><a class="nav-link text-warning" href="/admin">Admin</a></li><% } %>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4">

        <div class="banner-imagem my-4 text-center">
            <img src="/img/baner2.jpg" 
                 alt="Mega Promoção de Natal - Mais de 50% de Desconto" 
                 class="img-fluid rounded shadow-sm">
        </div>
        <section id="categorias" class="my-5">
            <h2 class="mb-4 text-center" data-aos="fade-up">Navegue por Categorias</h2>
            <div class="row g-4">
                <div class="col-md-4" data-aos="fade-up" data-aos-delay="100">
                    <a href="#" class="category-card d-block">
                        <i class="bi bi-tree category-icon"></i>
                        <h4 class="mt-3 mb-0 fw-bold">Árvores</h4>
                    </a>
                </div>
                <div class="col-md-4" data-aos="fade-up" data-aos-delay="200">
                     <a href="#" class="category-card d-block">
                        <i class="bi bi-snow2 category-icon"></i>
                        <h4 class="mt-3 mb-0 fw-bold">Decoração</h4>
                    </a>
                </div>
                <div class="col-md-4" data-aos="fade-up" data-aos-delay="300">
                     <a href="#" class="category-card d-block">
                        <i class="bi bi-brightness-high-fill category-icon"></i>
                        <h4 class="mt-3 mb-0 fw-bold">Luzes</h4>
                    </a>
                </div>
            </div>
        </section>

        <section id="produtos" class="my-5">
            <h2 class="mb-4" data-aos="fade-up">Produtos em Destaque</h2>
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-4">
                
                <% produtos.forEach((produto, index) => { %>
                    <div class="col" data-aos="fade-up" data-aos-delay="<%= (index % 4) * 100 %>">
                        <div class="card h-100 shadow-sm">
                            
                            <% if (produto.desconto_percentual) { %>
                                <span class="sale-badge">-<%= produto.desconto_percentual %>%</span>
                            <% } %>

                            <img src="<%= produto.imagem_capa %>" class="card-img-top" alt="<%= produto.nome %>" style="height: 200px; object-fit: cover;">
                            <div class="card-body">
                                <h5 class="card-title"><a href="/produto/<%= produto.id %>" class="link-produto"><%= produto.nome %></a></h5>
                                
                                <% if (produto.preco_promocional && produto.preco_promocional > 0) { %>
                                    <p class="card-text fs-6 text-muted mb-0">De: <del>R$ <%= produto.preco.toFixed(2) %></del></p>
                                    <p class="card-text fs-4 fw-bold text-success">Por: R$ <%= produto.preco_promocional.toFixed(2) %></p>
                                <% } else { %>
                                    <p class="card-text fs-4 fw-bold text-success mt-4"> R$ <%= produto.preco.toFixed(2) %></p>
                                <% } %>
                            </div>
                            <div class="card-footer bg-white border-top-0">
                                <button class="btn btn-primary w-100 btn-comprar" data-product-id="<%= produto.id %>"><i class="bi bi-cart-plus"></i> Comprar</button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </section>
    </main>

    <div class="cart-overlay" id="cart-overlay"></div>
    <aside class="cart-sidebar" id="cart-sidebar">
        <header class="cart-header"><h2 class="h5 mb-0">Meu Carrinho</h2><button id="cart-close-btn">&times;</button></header>
        <div class="cart-items-list" id="cart-items-list"></div>
        <footer class="cart-footer">
            <div class="cart-total"><span>Total:</span><span id="cart-total-price">R$ 0,00</span></div>
            <a href="/checkout" class="btn btn-success w-100 mb-2">Finalizar Compra</a>
            <button id="btn-limpar-carrinho" class="btn btn-outline-danger w-100">Limpar Carrinho</button>
        </footer>
    </aside>

    <footer class="footer-custom">
        <div class="container">
            <div class="row g-4">
                <div class="col-lg-4 col-md-6">
                    <a class="navbar-brand fs-2" href="/"><%= locals.lojaNome %></a>
                    <p class="my-3">A sua loja completa para um Natal mágico e inesquecível.</p>
                    <div class="footer-social-icons">
                        <a href="#" class="text-white"><i class="bi bi-instagram"></i></a>
                        <a href="#" class="text-white"><i class="bi bi-facebook"></i></a>
                        <a href="#" class="text-white"><i class="bi bi-whatsapp"></i></a>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6">
                    <h5>Links Rápidos</h5>
                    <ul class="list-unstyled">
                        <li><a href="/">Página Inicial</a></li>
                        <li><a href="/minha-conta">Minha Conta</a></li>
                        <li><a href="#produtos">Produtos em Destaque</a></li>
                        <% if (locals.isAdmin) { %>
                            <li><a href="/admin">Painel Admin</a></li>
                        <% } %>
                    </ul>
                </div>
                <div class="col-lg-4">
                    <h5>Contato</h5>
                    <ul class="list-unstyled">
                        <li><i class="bi bi-envelope-fill me-2"></i> contato@brilhodenatal.com</li>
                        <li><i class="bi bi-telephone-fill me-2"></i> (11) 99999-8888</li>
                        <li><i class="bi bi-geo-alt-fill me-2"></i> Rua Fictícia, 123 - São Paulo, SP</li>
                    </ul>
                </div>
            </div>
            <div class="footer-bottom text-center">
                <p class="mb-0">&copy; <%= new Date().getFullYear() %> <%= locals.lojaNome %>. Todos os direitos reservados.</p>
            </div>
        </div>
    </footer>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
    
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
      AOS.init({
          duration: 600, // Duração da animação
          once: true      // Animar apenas uma vez
      });
    </script>
</body>
</html>