<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Mountains+of+Christmas:wght@700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <title><%= locals.titulo %> - <%= locals.lojaNome %></title>
</head>
<body class="bg-light">

    <div class="promo-bar">
        <i class="bi bi-truck"></i>
        <strong>FRETE GRÁTIS</strong> em compras acima de R$ 30,00!
    </div>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/"><%= locals.lojaNome %></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="cart-link-produto"><i class="bi bi-cart"></i> Carrinho <span class="badge bg-success" id="cart-counter" style="display: none;"></span></a>
                    </li>
                    <% if (locals.clienteLogado) { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownClienteProduto" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-person-circle"></i> Olá, <%= locals.clienteLogado.nome %>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownClienteProduto">
                                <li><a class="dropdown-item" href="/minha-conta">Minha Conta</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/logout-cliente">Sair</a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link" href="/login-cliente"><i class="bi bi-box-arrow-in-right"></i> Login</a></li>
                        <li class="nav-item"><a class="nav-link" href="/register"><i class="bi bi-person-plus-fill"></i> Cadastre-se</a></li>
                    <% } %>
                     <% if (locals.isAdmin) { %><li class="nav-item"><a class="nav-link text-warning" href="/admin">Admin</a></li><% } %>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4" data-aos="fade-in">
        <div class="mb-3">
            <a href="/" class="btn btn-outline-secondary btn-sm">
                <i class="bi bi-arrow-left"></i> Voltar para a Loja
            </a>
        </div>
        <div class="card shadow-sm border-0">
            <div class="card-body p-4 p-md-5">
                <div class="row g-5">
                    <div class="col-lg-6">
                        <div class="mb-3 text-center position-relative">
                            <% if (locals.desconto_percentual) { %>
                                <span class="sale-badge-large">-<%= locals.desconto_percentual %>%</span>
                            <% } %>
                            <img src="<%= locals.imagemPrincipal %>" alt="<%= produto.nome %>" class="img-fluid rounded shadow-sm w-100" id="main-product-image" style="max-height: 550px; object-fit: contain; border: 1px solid #eee;">
                        </div>
                        <% if (locals.imagens && locals.imagens.length > 1) { %>
                            <div class="d-flex justify-content-center gap-2 product-gallery-thumbnails">
                                <% locals.imagens.forEach((imagem, index) => { %>
                                    <img src="<%= imagem %>" alt="Miniatura <%= index + 1 %>" 
                                         class="img-thumbnail thumbnail-item <%= index === 0 ? 'active' : '' %>"
                                         style="width: 80px; height: 80px; object-fit: cover; cursor: pointer;">
                                <% }); %>
                            </div>
                        <% } %>
                    </div>
                    <div class="col-lg-6 d-flex flex-column">
                        <h1 class="display-5"><%= produto.nome %></h1>
                        <% if (produto.preco_promocional && produto.preco_promocional > 0) { %>
                            <p class="display-6 text-muted mb-0">De: <del>R$ <%= produto.preco.toFixed(2) %></del></p>
                            <p class="display-5 text-success fw-bold my-2">Por: R$ <%= produto.preco_promocional.toFixed(2) %></p>
                        <% } else { %>
                            <p class="display-5 text-success fw-bold my-3">R$ <%= produto.preco.toFixed(2) %></p>
                        <% } %>
                        <hr>
                        <h5 class="fw-bold mt-3">Descrição do Produto</h5>
                        <p class="lead" style="font-size: 1.1rem; line-height: 1.6;"><%= produto.descricao || 'Este produto não possui descrição.' %></p>
                        <div class="mt-auto pt-4">
                            <button class="btn btn-primary btn-lg w-100 btn-comprar" data-product-id="<%= produto.id %>">
                                <i class="bi bi-cart-plus"></i> Adicionar ao Carrinho
                            </button>
                            <a href="/" class="btn btn-outline-secondary w-100 mt-2">Continuar Comprando</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
    
    <div class="cart-overlay" id="cart-overlay"></div>
    <aside class="cart-sidebar" id="cart-sidebar">
         <header class="cart-header"><h2 class="h5 mb-0">Meu Carrinho</h2><button id="cart-close-btn">&times;</button></header>
         <div class="cart-items-list" id="cart-items-list"></div>
         <footer class="cart-footer">
             <div class="cart-total"><span>Total:</span><span id="cart-total-price">R$ 0,00</span></div>
             <a href="/checkout" class="btn btn-success w-100 mb-2">Finalizar Compra</a>
             <button id="btn-limpar-carrinho" class="btn btn-outline-danger w-100">Limpar Carrinho</button>
         </footer>
     </aside>
    
    <footer class="footer-custom">
        <div class="container">
            <div class="row g-4">
                <div class="col-lg-4 col-md-6">
                    <a class="navbar-brand fs-2" href="/"><%= locals.lojaNome %></a>
                    <p class="my-3">A sua loja completa para um Natal mágico e inesquecível.</p>
                    <div class="footer-social-icons">
                        <a href="#" class="text-white"><i class="bi bi-instagram"></i></a>
                        <a href="#" class="text-white"><i class="bi bi-facebook"></i></a>
                        <a href="#" class="text-white"><i class="bi bi-whatsapp"></i></a>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6">
                    <h5>Links Rápidos</h5>
                    <ul class="list-unstyled">
                        <li><a href="/">Página Inicial</a></li>
                        <li><a href="/minha-conta">Minha Conta</a></li>
                        <li><a href="#produtos">Produtos em Destaque</a></li>
                        <% if (locals.isAdmin) { %>
                            <li><a href="/admin">Painel Admin</a></li>
                        <% } %>
                    </ul>
                </div>
                <div class="col-lg-4">
                    <h5>Contato</h5>
                    <ul class="list-unstyled">
                        <li><i class="bi bi-envelope-fill me-2"></i> contato@brilhodenatal.com</li>
                        <li><i class="bi bi-telephone-fill me-2"></i> (11) 99999-8888</li>
                        <li><i class="bi bi-geo-alt-fill me-2"></i> Rua Fictícia, 123 - São Paulo, SP</li>
                    </ul>
                </div>
            </div>
            <div class="footer-bottom text-center">
                <p class="mb-0">&copy; <%= new Date().getFullYear() %> <%= locals.lojaNome %>. Todos os direitos reservados.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
    <script src="/js/product-gallery.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
      AOS.init({ duration: 600, once: true });
    </script>
</body>
</html>